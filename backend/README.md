# Backend

FastAPI backend

## Setup

1. Install [uv](https://docs.astral.sh/uv/getting-started/installation/).

2. Copy `.env` and fill in your key:
   ```
   GEMINI_API_KEY=your_key_here
   DATA_DIR=./data
   ```

3. Install dependencies:
   ```
   uv sync
   ```
